general:
  initialized: false
  mounted_image: default
  num_images: 1
images:
  default:
    attached_volume: tfgpu-volume
    container_mountpath: /notebooks
    host_mountpath: ~/
    jupyter_port: 8888
    local_port: 9206
    repository: tensorflow/tensorflow
    tag: latest-gpu-py3
